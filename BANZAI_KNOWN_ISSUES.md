# BANZAI Pose Pipeline — 未解決の課題（引き継ぎメモ）

このファイルは、`src/pages/BanzaiPosePage.jsx` と `src/lib/banzaiPipeline.js` の
デバッグ作業を別セッションに引き継ぐための記録。検証には主に、横たわる人物と
その前に膝をついて肩を掴む人物の2人構図の写真を使用した（ユーザーの手元にある
元画像。このリポジトリには含まれていない）。

## 不具合ではないと判断: 横たわっている本人の腕・手が描画されない

`verifyArmsRendered`（`src/lib/banzaiPipeline.js`）が「肩から先、腕/手首/手が
見えない」と繰り返し検出し、これまで「不具合」として扱っていたが誤りだった。

検証に使った写真は、黒髪の人物が横たわっている本人に跨がり、その肩に手を
置いている構図。この場合、本人が腕を頭上に上げれば、カメラから見て黒髪人物の
体に隠れて見えなくなるのは**物理的に当然**であり、描画の欠陥ではない。
`verifyArmsRendered`の警告は、欠陥の検出ではなく、単に「正しく遮蔽されている
状態」を説明していただけの可能性が高い。ユーザーから複数回指摘されていたのを
見落としていた。

**今後この構図で作業する場合、`verifyArmsRendered`の警告を無条件に「直すべき
不具合」として扱わないこと。** 遮蔽物が前面にいる構図では、腕の一部が隠れる
のが正しい結果であり得る。

## 直近の修正履歴（このセッションでmainに反映済み、PR #16〜#24）

- `restoreOccluder`の除外ロジックを何度も調整（全体削除→幅を絞る→開始点をずらす
  →幅で絞り直す、など）。最終的に、腕マスクを「修正前(`armsOldRegion`)」と
  「修正後」に分離し、遮蔽物復元時は`armsOldRegion`とだけ重なりを除外する設計に
  落ち着いている。
- 遮蔽物マスクの太らせ幅（`occCtx.lineWidth`）を縮小→効果なしと判明→revert済み。
  **この方向は的外れだったので再検討不要。**
- `armsOldRegion`の手首側の円の半径を1.1倍→0.5倍に縮小（PR #24、マージ済み）。
  これは**今回検証に使った1枚の画像にたまたま合わせた場当たり的な数値調整**であり、
  根本修正ではなかった。別の画像では、AIの手首座標推定の誤差やレイアウトの違いに
  よって、半径を縮小したことが裏目に出て、直す前の腕がゴーストとして残る
  （このセッション最初期に直したはずのバグ）が再発する可能性があった。

  **根本修正、実施済み。** `analyzePoseWithProvider`のPOSE_SCHEMAに`visibleArms`
  （`occluders`と同形式: `{side: 'left'|'right', polygon: [...]}`）を追加し、
  遮蔽物と同じAI呼び出し1回の中で「現在見えている腕・手の輪郭」もポリゴンで
  なぞらせるようにした。`createAutomaticPlan`は、その輪郭が取得できた側は
  `paintOldArmRegion`（`src/lib/banzaiPipeline.js`）でポリゴンをfill+stroke
  して`armsOldRegion`を作り、輪郭が取得できなかった側（完全遮蔽など）だけ
  従来の決め打ち形状（肩・肘・手首のストローク+手首円）にフォールバックする。
  `BanzaiPosePage.jsx`の手動補正（`addTargetCorridors`、詳細調整で肩・手先を
  手動でずらした場合の再計算）も同じ`autoPlanRef.current.visibleArmPolygons`
  を再利用するよう変更済み（この輪郭は元画像そのものからAIが直接トレースした
  ものなので、手動で肩・手先の点をずらしても座標的な前提が崩れない）。
  追加のAI呼び出しは発生していない。

  **未検証:** 実画像でのvisibleArmsトレース精度そのものは、このセッションでは
  検証できていない（`BANZAI_KNOWN_ISSUES.md`冒頭の教訓のとおり、テキスト説明を
  鵜呑みにせず実画像をピクセル単位で確認すること）。AIが輪郭を返さなかった側は
  静かに旧来のフォールバックへ落ちるため、`visibleArms`が期待通り機能している
  かは、`armsOldRegion`を可視化するなどして実画像で確認するのが望ましい。
- `verifyArmsRendered`（両腕が実際に描画されたかを別のAI呼び出しで検証する仕組み）
  を追加済み。判定に失敗しても結果は隠さず、⚠️警告付きで表示する設計（誤検出も
  あり得るため、ユーザーが実画像を見て判断できるようにしている）。

## この作業で得た教訓

- 決め打ちの数値（太さ・半径・比率）による調整は、1枚の検証画像に対しては
  効いても、別の画像では成立しない。テキスト検証AIの説明文を鵜呑みにせず、
  実際の画像をピクセル単位で確認してから原因を主張すること。
- 大きな構造変更（PR作成・マージ）は、着手前に方針を確認してから進めること。
  自己判断で連続して出す・マージするのを急ぐと、検証不足のまま本番に反映され、
  手戻りが増える。
