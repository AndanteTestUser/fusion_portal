# BANZAI Pose Pipeline — 未解決の課題（引き継ぎメモ）

このファイルは、`src/pages/BanzaiPosePage.jsx` と `src/lib/banzaiPipeline.js` の
デバッグ作業を別セッションに引き継ぐための記録。検証には主に、横たわる人物と
その前に膝をついて肩を掴む人物の2人構図の写真を使用した（ユーザーの手元にある
元画像。このリポジトリには含まれていない）。

## 不具合ではないと判断: 横たわっている本人の腕・手が描画されない

`verifyArmsRendered`（`src/lib/banzaiPipeline.js`）が「肩から先、腕/手首/手が
見えない」と繰り返し検出し、これまで「不具合」として扱っていたが誤りだった。

検証に使った写真は、黒髪の人物が横たわっている本人に跨がり、その肩に手を
置いている構図。この場合、本人が腕を頭上に上げれば、カメラから見て黒髪人物の
体に隠れて見えなくなるのは**物理的に当然**であり、描画の欠陥ではない。
`verifyArmsRendered`の警告は、欠陥の検出ではなく、単に「正しく遮蔽されている
状態」を説明していただけの可能性が高い。ユーザーから複数回指摘されていたのを
見落としていた。

**今後この構図で作業する場合、`verifyArmsRendered`の警告を無条件に「直すべき
不具合」として扱わないこと。** 遮蔽物が前面にいる構図では、腕の一部が隠れる
のが正しい結果であり得る。

## 遮蔽物の復元まわりの設計（現状、確定）

`restoreOccluder`は、かつて「腕マスクを『修正前(armsOldRegion)』と『修正後』に
分離し、遮蔽物復元時はarmsOldRegionと重なる部分だけ復元から除外する」という
設計で、その`armsOldRegion`の作り方（決め打ちの幾何学図形→AIトレースしたポリゴン
→ハイブリッドなど）を何度も調整していた（PR #16〜#29の履歴）。しかし
「armsOldRegionをどう見積もっても、それが遮蔽物の実体（脚など）とたまたま重なれば、
その部分の遮蔽物が復元されず消える」という問題が構造的に解決できなかった。

**根本原因は復元処理ではなく、その手前の腕編集マスクの作り方にあった。**
`createAutomaticPlan`が作る腕編集マスク(`arms`)は、肩・肘・手首を通る旧腕の経路と
新しいバンザイ方向の経路の両方を覆う。もし遮蔽物（跨がっている人物の脚・手など）が
この経路と画面上で重なっていたら、腕編集のAI呼び出し自体がその重なった部分を
「腕として描き直す対象」に含めてしまい、そこで遮蔽物のピクセルが消える。
`restoreOccluder`が後からどう除外ロジックを工夫しても、一度壊れたものを
「復元するかどうか」の調整では直せない——**遮蔽物は常に画像の最前面レイヤーに
来るべきなのに、腕編集が残した空白がそれより手前に来てしまう**、というレイヤー
順序の不具合だった。

**この修正(`subtractMask(arms, occluder)`で腕編集マスクから遮蔽物を差し引く)は、
実画像で検証した結果、破棄済み。** 遮蔽物が画面の広い範囲を占める構図（このファイル
冒頭の検証写真がまさにそう）では、腕の到達経路(肩→目標)が遮蔽物マスクと大きく
重なり、`subtractMask`がその重なった範囲をまるごと腕編集マスクから消してしまう。
その結果:

1. **「腕の方向」工程(腕生成そのもの)が壊れる。** 肩から目標までの編集範囲が
   分断されたマスクを画像生成AIに渡すことになり、AIが連続した腕を描けず、
   肩から離れた場所に手だけが浮いた、分断された結果を生成する。この不具合は
   遮蔽物の復元(`restoreOccluder`、「確認・反映」工程)より前の、腕生成の
   時点で既に起きている。
2. **「確認・反映」工程の最終結果にも、変換されていない元の腕に近い見た目が
   残る。** これはピクセルの合成漏れではない(疑似データで全工程を再現し検証済み
   ——`original`のピクセルが遮蔽物の外側に漏れることは一切ない)。実態は1と
   同じ原因で、分断されたマスクを渡されたAIが正しい変換に失敗し、**たまたま
   元の腕に近い見た目を新規に生成してしまっていた**、というAI生成品質の問題。

つまりこの修正は、「遮蔽物復元(`restoreOccluder`、確認・反映工程)だけに
閉じるべき変更」を、誤って「腕編集マスクの構築(腕の方向工程)」にまで及ぼして
しまっていた。**`arms`マスクは常に完全な形（分断なし）で画像生成AIに渡すべきで、
遮蔽物がらみの処理は`restoreOccluder`の中だけで完結させること。**

**現状に戻した設計:** `arms`マスクは遮蔽物によって一切削らない（常に連続した
編集範囲をAIに渡す）。`visibleArms`・`armsOldRegion`・`paintOldArmRegion`は
不要なままコードから削除済み。

**最終確定した`restoreOccluder`の設計。** 「太らせた遮蔽物マスクを無条件復元する
だけ」に戻した状態では、太らせ部分(安全マージン)がたまたま元の腕の位置と重なって
いた場合にゴーストが復活するリスクが理論上残っていた。これに対処するため、
`occluder`(AIのポリゴンを太らせたもの。遮蔽物の一時除去編集にのみ使用)とは別に、
**`occluderCore`(同じポリゴンを太らせずに塗りつぶしただけのもの)を`restoreOccluder`
専用の復元マスクとして追加**した。復元は常に`occluderCore`を無条件・丸ごと使う。
「元の腕の位置」という概念は一切使わない――`occluderCore`は分析プロンプトの
時点で「本人自身の体や服は遮蔽物に分類しない」と指示されているため、この
ポリゴンが本人の腕を含むことは構造的にあり得ず、それを復元してもゴーストが
復活しようがない。トレードオフは、AIのポリゴンの縁が実際の境界よりわずかに
内側だった場合、境界ぎりぎりの数ピクセルだけ復元されずソフトエッジになる
可能性があること(構造的な破綻ではなく美観上の軽微な妥協)。

手動補正(`BanzaiPosePage.jsx`の詳細調整で遮蔽物マスクを手で塗る場合)には
「太らせ/太らせなし」の区別が元々ないため、ユーザーが描いた筆致をそのまま
`occluderCoreRef`にもミラーして、自動検出分と同じ扱いにしている。

**実画像で検証したこと（このセッションでは疑似データによる直接検証のみ。実画像は
未実施）:**
1. 腕編集マスク(`arms`)は、遮蔽物が到達経路に大きく重なる条件でも、肩から
   目標まで全区間で連続して塗られていることをピクセルサンプリングで確認。
2. 元の腕=青・遮蔽物=緑・生成結果=赤に塗り分けた疑似データで全工程
   (遮蔽物一時除去→腕編集→`occluderCore`での復元)を再現し、最終結果に
   青(元の腕)が一切残らないことを確認(ゴースト再発なし)。
3. 同じ疑似データで、`occluderCore`の範囲内が最終結果で正しく緑(遮蔽物)に
   復元されていることを確認(脚欠損なし)。境界の数%だけ黄(背景再構成)との
   中間色があったが、これは`occluderCore`のアルファ値が中間だった
   アンチエイリアシング境界のみで、実害ではないことを確認済み。

**未検証:** 3点とも疑似データでの検証であり、実画像ではまだ確認できていない。
次にこの構図で作業する際、(1) 腕が正しく連続して生成されるか、(2) 遮蔽物
(脚・手など)が欠けずに最前面へ復元されるか、(3) 元の腕のゴーストが出ていないか、
(4) 遮蔽物の輪郭にソフトエッジが目立たないか、の4点を実画像で確認すること。

`verifyArmsRendered`（両腕が実際に描画されたかを別のAI呼び出しで検証する仕組み）
は引き続き有効。判定に失敗しても結果は隠さず、⚠️警告付きで表示する設計（誤検出も
あり得るため、ユーザーが実画像を見て判断できるようにしている）。

## 遮蔽物の輪郭内側にある「穴」（ユーザー指摘、実装済み・実画像未検証）

上記の検証写真（跨っている人物が本人の肩を掴む構図）で、`occluder`/`occluderCore`
の赤マスクを見たユーザーから、「腕の内側（跨っている人物の両腕に挟まれた隙間）まで
赤く塗られている。本来そこには本人のバンザイ腕が描画されるはずなのに」という指摘を
受けた。

**根本原因:** `createAutomaticPlan`は`analysis.occluders`の各要素につき、外側の輪郭
ポリゴン1本だけを`fill()`していた。実際の遮蔽物のシルエットには、脇の隙間・指の間
など、外側の輪郭の内側にあるのに遮蔽物ではない「穴」（背景や本人の体が透けて見える
部分）が存在し得るが、単一の外側ポリゴンをそのまま塗りつぶす実装には「穴」を表現する
手段が一切なかった。さらに`occluder`(復元用ではなく一時除去用)の側は縁を太らせる
(`occCtx.lineWidth`)ため、この太らせが穴をさらに狭める・塞ぐ方向に働く。

これは単なる見た目の話ではなく、`occluderCore`（無条件・丸ごと復元される側）にまで
「穴」が塗りつぶされて含まれていると、その部分は最終合成で常に遮蔽物側で強制的に
上書きされる。もしバンザイ腕の経路がこの「本来は穴であるべき場所」と重なっていたら、
腕生成AIがそこに正しく腕を描いても`restoreOccluder`が問答無用に遮蔽物へ戻してしまい、
腕が消える。遮蔽が広範囲・隙間だらけの構図ほど致命的になり得るとユーザーが指摘した
通り。

**対応:** 人物解析のレスポンススキーマ(`POSE_SCHEMA`)に、各`occluders`要素へ
`holes`(その遮蔽物の外側輪郭の内側にある、背景または本人の体が見えている「穴」の
ポリゴン配列)を追加。解析プロンプトにも「外側の輪郭の内側に隙間があれば、それぞれ
独立したポリゴンとして`holes`に含めること」という指示を追加した。
`createAutomaticPlan`は、全`occluders`の外側ポリゴンを塗り終えた**あと**に、全ての
`holes`ポリゴンを`globalCompositeOperation = 'destination-out'`で`occluder`・
`occluderCore`の両方から打ち抜く（2パスに分けているのは、ある遮蔽物の穴を後続の
別の遮蔽物の塗りつぶしが再び埋めてしまうのを防ぐため）。穴は太らせ処理の影響を
受けないポリゴンそのままで打ち抜くため、`occluder`側の縁の太らせが塞いだ隙間も
ここで開け直される。

**未検証:** ここまでは実装のみで、実画像・実際のAIレスポンスでの検証はまだ。
次にこの構図で作業する際に確認すべきこと:
1. 人物解析AIが実際に`holes`へ妥当なポリゴンを返すか（プロンプトを追加しただけで、
   モデルが指示通り腕の間の隙間を認識してくれるとは限らない）。
2. `holes`が返された場合、その部分が`occluder`/`occluderCore`から正しく除外され、
   最終結果でバンザイ腕が遮蔽物に上書きされず見えるようになるか。
3. `holes`が返されなかった場合（モデルが隙間を見つけられなかった場合）は従来通り
   ユーザーが「自動範囲に問題がある場合だけ手動補正」の「－ 範囲を除外」で個別に
   直す必要がある——この機能は自動検出を完全に代替するものではなく、あくまで
   自動検出の精度を底上げするもの。

## 「確認・反映」工程の消しゴム機能（ユーザー提案、実装済み）

上記の`occluderCore`によるソフトエッジ、および遮蔽物判定・境界線のわずかな
ずれは、遮蔽物検出や腕変換そのものは正しく機能していても原理的に起こりうる
（AIのポリゴン精度や、影などで境界があいまいな場合）。「最終結果の画面で、
どちらのレイヤー（遮蔽物 or 対象人物）を前面にするかを、ユーザーが後から
直接調整できるようにすべき」という提案を受けて実装した。

**仕組み:** `restoreOccluder`が最後に行っているのは「`occluderCore`の範囲だけ
`original`を`pendingRef.current`（腕編集済み・復元前の合成結果）の上に重ねる」
という単純な合成。この2つのレイヤー（`original`と腕編集済み合成結果）は
既にどちらも手元にある（AI呼び出し不要）ので、`occluderCore`相当のマスクを
「確認・反映」画面でブラシ編集させ、その場で`restoreOccluder`を再実行する
だけで、AIを一切呼ばずに境界線を微調整できる。

- `preRestoreRef`（復元前の合成結果）と`finishMaskRef`（復元に使うマスクの
  編集用コピー）を、自動フロー・詳細調整フローどちらの`restoreOccluder`
  呼び出し直前にも保存するようにした。
- 「－ 消去」: 誤って前面(遮蔽物)になっていた部分を消し、奥の腕側を露出させる。
- 「＋ 追加」: 自動検出が遮蔽物を過小に検出していた場合の逆方向の補正。
- 「確認・反映」(`stage === 'done'`)の画面に直接「仕上がりを微調整」ボタンを
  置き、詳細調整モードに入らなくても使える。

**検証済み（実際のブラウザでE2E実行、OpenAI API呼び出しはモック）:** 元画像に
既知の色を仕込み、遮蔽物復元AI・腕編集AIの応答もそれぞれ別の既知の色になる
ようモックした上で、パイプラインを最後まで実行。「確認・反映」画面で消しゴム
ストロークを行い、(1) 該当ピクセルの色が遮蔽物色→腕編集色へ正しく切り替わる
こと、(2) 消しゴム操作の前後でネットワークリクエスト数が一切増えないこと
（完全にローカルで再合成されている）、(3) コンソールエラーが出ないこと、を
確認済み。ただし実際のユーザー画像・実際のAI応答での確認はまだ。

## この作業で得た教訓

- 決め打ちの数値（太さ・半径・比率）による調整は、1枚の検証画像に対しては
  効いても、別の画像では成立しない。テキスト検証AIの説明文を鵜呑みにせず、
  実際の画像をピクセル単位で確認してから原因を主張すること。
- 大きな構造変更（PR作成・マージ）は、着手前に方針を確認してから進めること。
  自己判断で連続して出す・マージするのを急ぐと、検証不足のまま本番に反映され、
  手戻りが増える。
- 「除外ロジックの範囲をどう見積もるか」という土俵で不具合を直そうとし続け、
  何度も同じ種類の不具合（ゴースト↔欠損のトレードオフ）を行き来していた。
  本当の原因は除外ロジックではなく、その手前で2つのマスク（腕編集・遮蔽物）が
  そもそも重なっていたこと。**「どちらを優先して除外するか」を工夫する前に、
  「そもそも重ならないようにできないか」を疑うこと。**
- 「常にこのレイヤーが最前面に来るべきだ」という不変条件があるなら、後段の
  合成処理で条件分岐して守ろうとするより、前段でその不変条件に反する入力
  （他のレイヤーがその領域を触ること）自体を構造的に発生させない方が、
  例外なく保証できる。**ただし、これは修正の影響範囲を正しく見切れている場合に
  限る。** 「遮蔽物復元(確認・反映)だけに閉じるべき修正」のつもりで腕編集マスク
  (`arms`)自体を書き換えたことで、その`arms`マスクを使う別の工程(腕の方向、
  つまり画像生成AIへのリクエストそのもの)にまで意図せず影響が及んだ。ある値を
  変更する前に「この値は他にどこで使われているか」を洗い出さずに直すと、
  直したつもりの工程とは別の工程を壊す。
- 合成ロジック自体が正しいか(ピクセルが漏れていないか)を検証したいときは、
  実画像でなくても、各レイヤーに識別しやすい単色(例: 元の腕=青、遮蔽物=緑、
  生成結果=赤)を割り当てた疑似データで全工程を再現し、最終合成結果にどの色が
  残るかを機械的に調べるのが有効。今回この手法で「ピクセルの合成漏れではなく、
  AIが分断されたマスクを渡されて変換に失敗しただけ」と、原因の切り分けが
  ピクセル単位で確定できた。
