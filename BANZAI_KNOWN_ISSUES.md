# BANZAI Pose Pipeline — 未解決の課題（引き継ぎメモ）

このファイルは、`src/pages/BanzaiPosePage.jsx` と `src/lib/banzaiPipeline.js` の
デバッグ作業を別セッションに引き継ぐための記録。検証には主に、横たわる人物と
その前に膝をついて肩を掴む人物の2人構図の写真を使用した（ユーザーの手元にある
元画像。このリポジトリには含まれていない）。

## 未解決: 横たわっている本人の腕・手が描画されない

`verifyArmsRendered`（`src/lib/banzaiPipeline.js`）が「肩から先、腕/手首/手が
見えない」と繰り返し検出する。実際に復元後の画像をピクセル単位で確認し、
誤検出ではなく実際に本人の袖・腕が肩から一切描画されていないことを確認済み
（テキスト説明だけでなく画像でも確認した）。

**原因は未確定。候補は以下の2つ、どちらか一方または両方が影響している可能性がある:**

1. **腕編集マスク(`plan.arms`)が「修正前の腕」と「修正後の目標」を1枚のマスクに
   混在させている。** `createAutomaticPlan`で、肩を起点に(a)肩→肘→手首(消去対象)
   と(b)肩→新目標(描画対象)を同じキャンバスにY字型に描き、1回のAI編集呼び出しで
   まとめて送っている。AIに「ここは消す・ここは新しく手を描く」という区別がなく、
   結果として手が曖昧にしか描かれない可能性がある。

2. **遮蔽物の復元が新しい手の位置と重なる。** バンザイの新しい手の目標位置が、
   前面の遮蔽人物（顔・髪など）の実際の位置と画面上で重なっていると、
   `restoreOccluder`が遮蔽物を正しく復元した結果として、新しく描いた手の上に
   本来そこにある物（遮蔽物の一部）が上書きされる。これは「バグ」ではなく
   物理的に正しい合成結果である可能性がある。

**まだ試していないこと:**
- 復元前（`restoreOccluder`を呼ぶ前）の腕生成結果だけを単独で確認する
  （現在はコスト削減のため復元後の1回しか検証していない）。これができれば、
  上記2つの候補を切り分けられる。
- 遮蔽物が存在しない、1人だけの単純な画像で同じ処理を試し、腕マスクの
  Y字型の曖昧さだけを単独で検証する。

**提案していた根本修正（未着手）:** 人物解析のAI呼び出し（`analyzePoseWithProvider`）
に、遮蔽物と同様に「現在見えている腕・手の輪郭」もポリゴンとしてなぞらせ、
`armsOldRegion`を決め打ちの幾何学図形（肩・肘・手首の3点から合成したストローク
+円）ではなく、実際にAIが検出した輪郭から作る。これにより、画像ごとに異なる
腕と遮蔽物の距離関係に依存しない、原理的に正しい除外エリアになるはず。
追加のAI呼び出しは不要（既存の解析呼び出しに項目を追加するだけ）。

## 直近の修正履歴（このセッションでmainに反映済み、PR #16〜#24）

- `restoreOccluder`の除外ロジックを何度も調整（全体削除→幅を絞る→開始点をずらす
  →幅で絞り直す、など）。最終的に、腕マスクを「修正前(`armsOldRegion`)」と
  「修正後」に分離し、遮蔽物復元時は`armsOldRegion`とだけ重なりを除外する設計に
  落ち着いている。
- 遮蔽物マスクの太らせ幅（`occCtx.lineWidth`）を縮小→効果なしと判明→revert済み。
  **この方向は的外れだったので再検討不要。**
- `armsOldRegion`の手首側の円の半径を1.1倍→0.5倍に縮小（PR #24、マージ済み）。
  これは**今回検証に使った1枚の画像にたまたま合わせた場当たり的な数値調整**であり、
  根本修正ではない。**別の画像では、AIの手首座標推定の誤差やレイアウトの違いに
  よって、半径を縮小したことが裏目に出て、直す前の腕がゴーストとして残る
  （このセッション最初期に直したはずのバグ）が再発する可能性がある。**
  次に着手する場合、上記の「AIに実際の腕の輪郭をなぞらせる」根本修正で、
  この場当たり的な数値そのものを置き換えることを推奨する。
- `verifyArmsRendered`（両腕が実際に描画されたかを別のAI呼び出しで検証する仕組み）
  を追加済み。判定に失敗しても結果は隠さず、⚠️警告付きで表示する設計（誤検出も
  あり得るため、ユーザーが実画像を見て判断できるようにしている）。

## この作業で得た教訓

- 決め打ちの数値（太さ・半径・比率）による調整は、1枚の検証画像に対しては
  効いても、別の画像では成立しない。テキスト検証AIの説明文を鵜呑みにせず、
  実際の画像をピクセル単位で確認してから原因を主張すること。
- 大きな構造変更（PR作成・マージ）は、着手前に方針を確認してから進めること。
  自己判断で連続して出す・マージするのを急ぐと、検証不足のまま本番に反映され、
  手戻りが増える。
